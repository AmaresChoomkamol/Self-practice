Week 1

ฝึกเขียน JavaScript Array 
การ loop
การเพิ่ม ลบข้อมูล
การใช้ method ต่างๆ
การจัดการ array ซ้อนกัน
การแปลงค่าระหว่าง string กับ array
การใช้ spread operator
สร้าง array แบบต่างๆ Array.of Array.from new Array()

Week 2

ฝึกเขียน JavaScript object 
สร้าง object
ใช้ method
เปรียบเทียบ object
ใช้ class
clone object
destructuring
shallow equality

Week 3

ฝึกเขียน JavaScript Function
รูปแบบ function declaration expression arrow function
ส่ง function เป็น argument
return function จาก function
ใช้ default parameter
ใช้ rest parameter และ arguments
ใช้ spread operator
destructuring 

Week 4

ฝึกใช้ Anonymous Function และ Callback
ใช้ filter() คัดข้อมูลจาก array
ใช้ map() แปลงค่าทุกตัวใน array
ใช้ forEach() ทำงานกับทุก element ใน array
ใช้ sort() เรียงลำดับ เปลี่ยนค่าเดิมใน array
ใช้ reduce() รวมค่าหรือเปลี่ยนข้อมูลใน array ให้เป็นค่าเดียว
ฝึกการใช้ includes() some() ตรวจสอบเงื่อนไขใน array string
การจัดการ array ด้วย slice() splice() fill()
แปลง string เป็นตัวใหญ่ทั้งหมดด้วย toUpperCase() ผ่าน forEach()
สร้าง function แยกไว้ใช้ซ้ำกับ filter() callback แบบมีชื่อ

Week 5

ฝึกใช้ filter() และ map() ร่วมกัน
ใช้ for of loop กับเงื่อนไขในการกรองข้อมูล
ใช้ concat() รวม array 2 ชุดเข้าด้วยกัน
ใช้ toString() แปลง array เป็น string
เรียงลำดับข้อมูลใน array ด้วย sort()
เรียงตัวเลขแบบ numeric โดยใช้ callback (a,b) => a - b
เรียงชื่อใน array ของ object ด้วย localeCompare()
ใช้ join() รวมค่าทั้งหมดใน array เป็น string
ใช้ indexOf() หาค่าภายใน array โดยระบุตำแหน่งเริ่มต้น
CommonJS ใช้ require() เพื่อ import 
ES Modules ใช้ import export แบบ named และ default
เปลี่ยนชื่อ function ตอน import ด้วย as
